"""gimp-mcp — a GIMP 3.x MCP server.

A full tool surface (119 tools across 13 groups) over a hybrid live/headless
bridge, with structured returns, a GIMP-3 compat layer, safety checkpoints, and
print/DTF-aware tooling. Run the server with ``gimp-mcp serve``.
"""

__version__ = "0.3.1"
